/**
 * @deprecated Use functions from @/lib/supabase/client directly
 * This file is kept for backward compatibility
 */
import { 
  getProducts, 
  getProductBySlug, 
  createOrder, 
  createOrderItems,
  addSubscriber
} from './supabase/client';

export { 
  getProducts, 
  getProductBySlug, 
  createOrder, 
  createOrderItems,
  addSubscriber
}; 